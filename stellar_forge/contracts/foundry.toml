[profile.default]
src = "."
test = "test"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200

# Setup:
#   cd stellar_forge/contracts
#   forge install foundry-rs/forge-std --no-commit
#   forge test -vvv
#
# Deploy (testnet first — Base Sepolia):
#   forge create Supernova.sol:Supernova \
#     --constructor-args 2000 1000000000 \
#     --rpc-url $BASE_SEPOLIA_RPC --private-key $DEPLOYER_KEY
