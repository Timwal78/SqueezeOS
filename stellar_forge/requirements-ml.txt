# Optional ML/crypto deps for the Stellar Forge ML + fusion layer.
# DELIBERATELY separate from the repo's top-level requirements.txt so the
# production trading deploy (Render/Docker) is NOT bloated with torch.
#
#   pip install -r stellar_forge/requirements-ml.txt
#
# The economy + routing layer (economy/, gateway/) needs NONE of these —
# it runs on the Python stdlib alone.

torch>=2.0          # fusion_engine, shard_router, black_hole (research)
pyyaml>=6.0.3         # lora_merge mergekit config generation
pycryptodome>=3.19  # shard_router keccak256 (matches Solidity exactly)
mergekit>=0.1.4     # lora_merge production adapter fusion (run step)
