{
  "name": "neural-os",
  "version": "1.0.0",
  "description": "Neural_OS — Institutional RWA Agentic Infrastructure",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap run android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
