name: xdeo-ci

on:
  push:
    branches: ["**"]
    paths: ["xdeo/**"]
  pull_request:
    paths: ["xdeo/**"]

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: xdeo
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-node@cdca7d6dd16c72fb5d9c5b2d47aaddfca8fd94e2 # v4
        with:
          # Node >= 22.5 is required for the built-in node:sqlite module used by
          # the DB integration tests (test/helpers/d1.ts).
          node-version: "22"
          cache: npm
          cache-dependency-path: xdeo/package-lock.json
      - run: npm ci
      - run: npm run typecheck
      - run: npm test
