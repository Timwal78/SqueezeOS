# Service Scaffold

This is a minimal working FastAPI reference for ARGUS OMEGA.
Antigravity should expand this into the full production codebase using the master spec.

Run locally:
```bash
pip install -r requirements.txt
uvicorn main:app --reload
```
